<?php

declare(strict_types=1);

/**
 * Configuration locale d'environnement — MODÈLE.
 *
 * Copier ce fichier en config/env.local.php (non versionné) et adapter les valeurs.
 * Seules les clés à surcharger doivent être présentes ; elles sont fusionnées
 * avec config/app.php. Les variables d'environnement ATELIER_* ont ensuite la priorité
 * (ex. ATELIER_APP_DEBUG=0, ATELIER_DATABASE_DRIVER=mysql).
 */
return [
    'app' => [
        // config/app.php est réglé pour la production. À décommenter sur un poste de
        // développement seulement : le mode debug renvoie au client les traces d'appel,
        // les chemins de fichiers et les requêtes SQL.
        // 'env' => 'dev',
        // 'debug' => true,
        // 'base_url' => '/atelier',
    ],
    'database' => [
        'driver' => 'sqlite',
        // 'sqlite' => ['path' => '/chemin/absolu/vers/atelier.sqlite'],
        // 'mysql' => ['host' => '127.0.0.1', 'name' => 'atelier', 'user' => 'atelier', 'password' => 'secret'],
    ],
    'session' => [
        // 'cookie_secure' => true,
    ],
];
